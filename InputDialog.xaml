<Window x:Class="WpfApp1.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="{Binding Title}" Height="180" Width="450"
     WindowStartupLocation="CenterOwner" 
    WindowStyle="ToolWindow"
   ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
 <Grid.RowDefinitions>
   <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
   <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

   <TextBlock Text="{Binding Question}" Grid.Row="0" TextWrapping="Wrap" FontSize="14"/>
    
      <TextBox x:Name="txtAnswer" Grid.Row="1" Margin="0,10,0,0" Height="30" 
           FontSize="14" VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="2" Margin="0,5,0,0" FontSize="12" Foreground="Gray" TextWrapping="Wrap">
            Can be simple name like "backup" or path like "files/2024" for nested folders
        </TextBlock>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
   <Button Content="OK" Width="80" Height="30" Margin="0,0,10,0" 
       IsDefault="True" Click="BtnOk_Click"
            Background="#FF2196F3" Foreground="White" FontWeight="Bold"/>
            <Button Content="Cancel" Width="80" Height="30" 
     IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
